# Plan Cache 源码阅读计划索引（重点导向版）

目标不是背源码。目标是建立一套能解释 plan cache 行为的认知模型：

```text
入口判断 -> key 生成 -> 查找匹配 -> 命中执行
                    -> 未命中 hard parse -> add plan
                    -> 生命周期保护 -> 淘汰/flush/观测
```

## 阅读原则

1. 先看主链路，不追所有分支。
2. 每阶段只抓 3 类内容：核心问题、关键对象、验证方法。
3. 源码只读“能串起流程”的函数，不把所有实现细节背下来。
4. 重要点要能讲清楚“为什么这样设计”。
5. 每阶段结束都画一张图，回答一组问题。

## 阶段文件

1. `phase_01_overview.md`：建立全局地图，知道 plan cache 解决什么问题。
2. `phase_02_sql_hit.md`：理解一次 cache hit 怎么发生。
3. `phase_03_sql_miss_add.md`：理解 miss 后如何生成并加入计划。
4. `phase_04_key_match.md`：理解“能不能复用”的判定逻辑。
5. `phase_05_libcache_lifecycle_memory.md`：理解对象生命周期、引用计数、flush/evict。
6. `phase_06_ps_pl_observability_tests.md`：理解 PS/PL 扩展和观测验证。

## 每阶段固定模板

```text
本阶段重点：只需要理解什么。
不必深挖：哪些细节先跳过。
核心问题：读完必须能回答什么。
关键入口：只列最少源码入口。
理解模型：用一张图串起来。
验证方式：用实验或虚拟表证明。
阶段产出：图/表/结论。
```

## 总体验收

你不需要记住所有类成员，但要能说明：

- 为什么同一 SQL 第二次可能 hit。
- 为什么文本不同也可能复用同一计划。
- 为什么 key 相同仍可能不能复用。
- 为什么 add plan 失败通常不影响当前 SQL。
- 为什么 flush 不会释放正在执行的 plan。
- PS/PL 为什么能复用同一套 Lib Cache 框架。
- 观测视图的数据从哪里来，如何验证 hit/miss。

## 推荐节奏

- Day 1：阶段 1、2，先跑通 hit 主线。
- Day 2：阶段 3，补齐 miss/add 主线。
- Day 3：阶段 4，理解复用判定。
- Day 4：阶段 5，理解生命周期和淘汰。
- Day 5：阶段 6，用 PS/PL/虚拟表/测试闭环。
